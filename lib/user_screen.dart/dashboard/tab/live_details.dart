import 'package:flutter/material.dart';
import 'package:smart_auction/model/items_model.dart';

class LiveScreenDetail extends StatelessWidget {
  final Items? items;

  const LiveScreenDetail({Key? key, this.items}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
